# marker_var and marker_label are mutually exclusive

    Code
      geom_swimlane_marker(id, response, marker_var = arm, marker_label = "PR")
    Condition
      Error in `geom_swimlane_marker()`:
      ! Supply only one of `marker_var` or `marker_label`.

