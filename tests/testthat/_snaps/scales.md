# scale_shape_swimlane errors when unnamed values run short

    Code
      ggplot2::ggplot_build(p)
    Condition
      Error in `palette()`:
      ! `values` has 2 shapes but 3 are needed; name the values or supply enough shapes.

