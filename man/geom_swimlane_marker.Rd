% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_swimlane_marker.R
\name{geom_swimlane_marker}
\alias{geom_swimlane_marker}
\title{Mark on-lane events with shaped points}
\usage{
geom_swimlane_marker(
  id_var,
  time_var,
  marker_var = NULL,
  marker_label = NULL,
  size = 2.5,
  colour = "grey20",
  ...
)
}
\arguments{
\item{id_var}{Column with the subject identifier, mapped to the y-axis.}

\item{time_var}{Numeric column with the event time, mapped to the x-axis.
\code{NA} means no event for that subject.}

\item{marker_var}{Optional column mapped to shape, for data with several
event types. Cannot be combined with \code{marker_label}.}

\item{marker_label}{Optional string naming this event in the shape legend
(e.g. \code{"Partial response"}). Cannot be combined with \code{marker_var}.}

\item{size}{Point size.}

\item{colour}{Point color.}

\item{...}{Other arguments passed to \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}.}
}
\value{
A list containing a ggplot2 layer.
}
\description{
Draws a shaped point on each lane at the time of an event (e.g. partial
response). Subjects with \code{NA} in \code{time_var} are skipped. Supply
\code{marker_label} to add the marker to the shared shape legend, or
\code{marker_var} to map several event types to different shapes.
}
\examples{
library(ggplot2)

patient_disposition |>
  order_swimlane(subject, weeks_on_study, cohort) |>
  ggplot() +
  geom_swimlane(subject, weeks_on_study, cohort) +
  geom_swimlane_marker(
    subject, partial_response,
    marker_label = "Partial response"
  ) +
  labs(x = "Weeks on study") +
  theme_swimlane()
}
