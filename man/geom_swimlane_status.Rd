% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_swimlane_status.R
\name{geom_swimlane_status}
\alias{geom_swimlane_status}
\title{Mark end-of-lane status with shaped points}
\usage{
geom_swimlane_status(
  id_var,
  duration_var,
  status_var,
  ongoing = "On study",
  arrow = TRUE,
  size = 2.8,
  colour = "grey20",
  fill = colour,
  stroke = 0.75,
  halo = "white",
  ...
)
}
\arguments{
\item{id_var}{Column with the subject identifier, mapped to the y-axis.}

\item{duration_var}{Numeric column with the lane length, mapped to the
x-axis.}

\item{status_var}{Column with the status label. \code{NA} values are treated as
ongoing.}

\item{ongoing}{Legend label for ongoing subjects; also recognized as a
value of \code{status_var}.}

\item{arrow}{If \code{TRUE} (default), ongoing subjects get a rightward arrow
with a separate legend entry. If \code{FALSE}, the \code{ongoing} label is treated
as a regular status level in the shape legend.}

\item{size}{Point size; the ongoing arrow is drawn at \code{1.4 * size}.}

\item{colour}{Point color, used for both the outline and (by default) the
fill of the glyph, and for the ongoing arrow.}

\item{fill}{Fill color for the solid glyphs (shapes 21-25). Defaults to
\code{colour}.}

\item{stroke}{Outline width of the glyph.}

\item{halo}{Color of the ring drawn beneath each status glyph, separating
it from the bar fill. Use \code{NA} to disable (e.g. on dark backgrounds).}

\item{...}{Other arguments passed to \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}.}
}
\value{
A list of ggplot2 layers (and, when \code{arrow = TRUE}, a scale that
creates the arrow's legend entry).
}
\description{
Draws a shaped point at the end of each lane indicating the subject's
status (e.g. reason off study), with a legend generated automatically from
the shape mapping. Subjects still on study (\code{NA} in \code{status_var}, or the
value of \code{ongoing}) are drawn as a rightward arrow with its own legend
entry.
}
\examples{
library(ggplot2)

patient_disposition |>
  order_swimlane(subject, weeks_on_study, cohort) |>
  ggplot() +
  geom_swimlane(subject, weeks_on_study, cohort) +
  geom_swimlane_status(subject, weeks_on_study, reason_off_study) +
  labs(x = "Weeks on study") +
  theme_swimlane()
}
